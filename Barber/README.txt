1.Barber Front-end (HTML/CSS/Javascript)
     Auth 
     landing-page(barber)
     sidebar
     User-queue
2.Barber Back-end
     Database Design
     CSRF Authrization
     barber functionalites
       -> ON/OFF the shop.
        ->create a chair.
        ->displaying the queue of customer.
        ->create a partner.
        ->execute operations.
       ->choose a customer.
       ->notify the customer.
       ->estimated time.
